DROP DATABASE EMPLOYEE;

CREATE DATABASE EMPLOYEE;

USE EMPLOYEE;

CREATE TABLE Employee (
EMPNO INT PRIMARY KEY,
ENAME VARCHAR(50),
JOB VARCHAR(50),
MGR INT,
SAL DECIMAL(10, 2)
);

ALTER TABLE Employee
ADD COLUMN COMMISSION DECIMAL(10, 2) DEFAULT NULL;

INSERT INTO Employee (EMPNO, ENAME, JOB, MGR, SAL) VALUES
(101, 'John Doe', 'Manager', NULL, 50000),
(102, 'Jane Smith', 'Developer', 101, 40000),
(103, 'Jim Brown', 'Tester', 101, 35000),
(104, 'Jake White', 'Developer', 101, 42000),
(105, 'Laura Green', 'HR', 101, 45000);

SELECT * FROM Employee;

UPDATE Employee
SET JOB = 'Senior Developer'
WHERE ENAME = 'Jane Smith';

SELECT * FROM Employee;

ALTER TABLE Employee
CHANGE COLUMN COMMISSION COM DECIMAL(10, 2) DEFAULT NULL;

SELECT * FROM Employee; 

DELETE FROM Employee
WHERE EMPNO = 105;

SELECT * FROM Employee; 