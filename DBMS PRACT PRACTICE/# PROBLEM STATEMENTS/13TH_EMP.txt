DROP DATABASE EMPLOYEE;

CREATE DATABASE EMPLOYEE;

USE EMPLOYEE;

CREATE TABLE EMP (
EMPNO INT(6) NOT NULL,
ENAME VARCHAR(20) NOT NULL,
JOB VARCHAR(10) NOT NULL,
DEPTNO INT(3),
SAL DECIMAL(7, 2),
CONSTRAINT PK_EMPNO PRIMARY KEY (EMPNO),
CONSTRAINT CHK_EMPNO CHECK (EMPNO > 100),
CONSTRAINT UQ_DEPTNO UNIQUE (DEPTNO)
);

INSERT INTO EMP (EMPNO, ENAME, JOB, DEPTNO, SAL)
VALUES (101, 'John Doe', 'Manager', 10, 50000);

INSERT INTO EMP (EMPNO, ENAME, JOB, DEPTNO, SAL)
VALUES (102, 'Jane Smith', 'Developer', 20, 40000);

INSERT INTO EMP (EMPNO, ENAME, JOB, DEPTNO, SAL)
VALUES (103, 'Charlie Brown', 'Analyst', 30, 60000);

SELECT * FROM EMP;


INSERT INTO EMP (EMPNO, ENAME, JOB, DEPTNO, SAL)
VALUES (104, 'Eve White', 'Designer', 40, 45000); 

SELECT * FROM EMP;

UPDATE EMP
SET SAL = 55000
WHERE EMPNO = 101;

SELECT* FROM EMP;

UPDATE EMP
SET DEPTNO = 10
WHERE EMPNO = 103;

SELECT* FROM EMP;

DELETE FROM EMP
WHERE EMPNO = 102;

SELECT* FROM EMP;

SELECT * FROM EMP
WHERE EMPNO > 100;

SELECT DEPTNO, COUNT(*) 
FROM EMP
GROUP BY DEPTNO;
SELECT * FROM EMP;
